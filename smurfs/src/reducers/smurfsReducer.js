const initialState = {

    smurfs : [],
    title : 'HELLLLLOOOOO'

}

export const reducer = ( state = initialState, action) => {
    switch(action.type){
        default:
            return state
    }
}